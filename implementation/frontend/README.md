# Frontend (Static)

Bu oddiy demo frontend. Vercelga to'g'ridan-to'g'ri static sifatida deploy qilinadi.

## Local test

```bash
cd implementation/frontend
python -m http.server 8080
```

Brauzerda `http://localhost:8080` oching.
